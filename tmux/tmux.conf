# --- 프리픽스 변경: C-b -> C-Space ---
unbind C-b
set -g prefix C-Space
bind C-Space send-prefix

# --- 클립보드 연동 ---
# nvim 등에서 "+ 레지스터 yank가 시스템 클립보드로 가도록 OSC52 허용
set -g set-clipboard on

# --- 마우스 ---
# 마우스 휠 스크롤 + 드래그 선택 가능
set -g mouse on

# --- 스크롤백 버퍼 크게 ---
set -g history-limit 50000

# --- copy 모드를 vim 키로 ---
setw -g mode-keys vi

# copy 모드에서 v로 선택 시작, y로 시스템 클립보드에 복사 (wl-copy 사용)
bind-key -T copy-mode-vi v   send-keys -X begin-selection
bind-key -T copy-mode-vi y   send-keys -X copy-pipe-and-cancel 'wl-copy'
bind-key -T copy-mode-vi MouseDragEnd1Pane send-keys -X copy-pipe-and-cancel 'wl-copy'

# 터미널 색/이스케이프 제대로 통과 (truecolor)
set -ga terminal-overrides ',*:Tc'
set -g focus-events on

# --- vim-tmux-navigator ---
# Ctrl+h/j/k/l 로 prefix 없이 nvim 분할창 <-> tmux 패널 이동
# 현재 패널이 vim/nvim 이면 키를 vim 에 넘기고, 아니면 tmux 패널 이동
is_vim="ps -o state= -o comm= -t '#{pane_tty}' \
    | grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?(view|l?n?vim?x?|fzf)(diff)?$'"
bind-key -n 'C-h' if-shell "$is_vim" 'send-keys C-h'  'select-pane -L'
bind-key -n 'C-j' if-shell "$is_vim" 'send-keys C-j'  'select-pane -D'
bind-key -n 'C-k' if-shell "$is_vim" 'send-keys C-k'  'select-pane -U'
bind-key -n 'C-l' if-shell "$is_vim" 'send-keys C-l'  'select-pane -R'

# C-l 이 패널 이동에 묶여 셸 clear 가 막히므로 prefix + C-l 로 clear 복원
bind C-l send-keys 'C-l'

bind-key -T copy-mode-vi 'C-h' select-pane -L
bind-key -T copy-mode-vi 'C-j' select-pane -D
bind-key -T copy-mode-vi 'C-k' select-pane -U
bind-key -T copy-mode-vi 'C-l' select-pane -R

# --- 새 윈도우/패널을 현재 경로 기준으로 열기 ---
bind c  new-window      -c "#{pane_current_path}"
bind '%' split-window -h -c "#{pane_current_path}"
bind '"' split-window -v -c "#{pane_current_path}"

# --- 3패널 가로 비율 고정: 24% / 27% / 49% ---
# prefix + = 로 항상 같은 기준으로 정렬 (마지막 패널은 나머지 자동)
bind = resize-pane -t .0 -x 24% \; resize-pane -t .1 -x 27%

# --- 키보드 스크롤 ---
# Alt+u 한 번으로 prefix 없이 스크롤 모드 진입 + 반페이지 위로
bind -n M-u copy-mode \; send-keys -X halfpage-up
# 스크롤 모드(vi)에서 Ctrl+u / Ctrl+d 반페이지 이동
bind -T copy-mode-vi C-u send-keys -X halfpage-up
bind -T copy-mode-vi C-d send-keys -X halfpage-down
